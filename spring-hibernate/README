Тестовое приложение для участия в онлайн-стажировке.

Для запуска необходимо:
1. Склонировать репозиторий на свой комп.
2. Импортировать его в IDE (для создания я использовал Eclipse, запускать пробовал и на нем, и в IDEA).
3. Запустить скрипт base_and_table_prepare.sql из корня проекта
    (он создает базу с названием test и сразу заполняет ее сотней фейковых записей).
4. Задеплоить и запустить приложение на сервере.
